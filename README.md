# todo-lists
1. To setup run in your terminal

    ```bash
    $ make
    ```
   If you can't run 'make' for some reasons, install it locally also you can check out *docker-compose* commands in Makefile and up the project manually using docker.